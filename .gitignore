# Raw downloaded assets from Wix
assets/raw/

# OS Files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*

# Temporary files
*.tmp
*.temp

# Environment variables
.env
.env.local
.env.*.local

# Service account credentials (NEVER commit these!)
google-service-account.json
*service-account*.json
*credentials*.json

# Python virtual environment
venv/
Venv/
env/
ENV/
.venv/

# Node.js / NPM
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
# Note: Keeping package.json and package-lock.json (they're small and useful for setup)


# YouTube API credentials - DO NOT COMMIT
scripts/youtube_credentials.json
**/credentials.json
**/client_secret*.json

# YouTube OAuth token
youtube_token.json
**/youtube_token.json

# YouTube video mappings (contains IDs)
scripts/youtube_videos.json

# Video files (hosted on YouTube, embedded via iframes)
# These videos are uploaded to YouTube and embedded, so we don't need to commit them
assets/videos/*.mp4
assets/videos/*.MP4
